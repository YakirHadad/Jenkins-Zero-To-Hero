pipeline {
    agent {
        docker {
            // Use a Node.js Docker image
            image 'node:14'
        }
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                script {
                    // Run npm install
                    sh 'npm install'
                }
            }
        }

        stage('Build') {
            steps {
                script {
                    // Your build steps, for example: npm run build
                    sh 'npm run build'
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    // Your test steps, for example: npm test
                    sh 'npm test'
                }
            }
        }

        stage('Deploy') {
            steps {
                // Your deployment steps
                // This section is a placeholder; customize it based on your deployment strategy
            }
        }
    }
}
